require 'rubygems'
require 'rake'
require 'jeweler'

Jeweler::Tasks.new do |gem|
  gem.name = "flati"
  gem.homepage = "http://github.com/daveed/flati"
  gem.license = "MIT"
  gem.summary = "flati wraps and installs the bootstrap theme Flati into a rails asset pipeline."
  gem.description = "flati wraps and installs the bootstrap theme Flati into a rails asset pipeline."
  gem.email = "david.alphen@gmail.com, carl@house-keeping.com"
  gem.authors = ["daveed, housekeeping"]
end
Jeweler::RubygemsDotOrgTasks.new
